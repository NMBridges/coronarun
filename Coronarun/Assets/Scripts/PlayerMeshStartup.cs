using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerMeshStartup : MonoBehaviour
{
    MeshFilter mf;

    void Awake()
    {
    	//mf = GetComponent<MeshFilter>();
    	
    }

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //mf.mesh = GetComponent<SkinnedMeshRenderer>().sharedMesh;
    }
}
